import * as selector from 'd3-selection';

export default class constructionPattern{
    private parentSelector: any;


    public setParentSelector(selector: any){
        this.parentSelector = selector;
    }
}